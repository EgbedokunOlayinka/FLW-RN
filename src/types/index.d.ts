export interface IInventoryItem {
  name: string;
  price: number;
  totalStock: number;
  description: number;
  user: string;
  id: string;
}

export interface IUser {
  email: string;
  password: string;
}
